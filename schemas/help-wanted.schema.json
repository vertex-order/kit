{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "help-wanted.schema.json",
  "description": "The window.HELP_WANTED_ITEMS value in site/data/help-wanted.js. HelpWanted.dc.html renders each item as a bare <li> -- flat strings only, no `{parts}`, no `id`, no richer shape.",
  "type": "array",
  "items": { "type": "string", "minLength": 1 }
}
